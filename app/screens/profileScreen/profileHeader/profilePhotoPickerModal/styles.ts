import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  overlay: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  container: {
    width: 300,
    padding: 16,
    borderRadius: 20,
    alignItems: 'center',
    gap: 12,
  },
  image: {
    width: 150,
    height: 150,
    borderRadius: 75,
  },
});